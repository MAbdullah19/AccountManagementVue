// Firebase project config. Filled in at Phase 1 from the Firebase console
// (Project settings -> General -> Your apps -> Web app).
//
// This file is committed on purpose. The web API key is not a secret: it
// identifies the project, it does not authorise anything. Security comes from
// the Realtime Database rules (see README). Do not build a scheme to hide it.
export const firebaseConfig = {};
